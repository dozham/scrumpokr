services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  app:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - redis
    environment:
      - NODE_ENV=production
      - PORT=3000
      - ROOM_STORE_ADAPTER=redis
      - REDIS_URL=redis://redis:6379
